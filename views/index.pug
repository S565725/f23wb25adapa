extends layout

block content
  h1= title
  p Welcome to #{title}
  p Hello index.pug
  
  button(type="button", class="btn btn-secondary" onclick = "goFaster()") Press me for the content.
  #faster Increase the speed.
  a(href='/yacht') View results
  script.
   var mph = 20;
   var change = 1;
   function goFaster() {
     mph += change;
     change += 1;
     document.getElementById('faster').innerText = `MPH ${mph} with change ${change}`;
   }
  

  
 
  
  
  